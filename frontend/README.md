# LMS Frontend (Auth-enabled)

Setup:
1. cd frontend
2. npm install
3. npm run dev

The frontend now includes Login and Register pages. After registering or logging in, the JWT token is saved to localStorage and automatically attached to API requests.
